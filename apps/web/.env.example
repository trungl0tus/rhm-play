# Copy to .env.local for local development; set the same key in the Vercel
# project's "Environment Variables" panel for preview & production.
#
# NEXT_PUBLIC_ prefix is required so Next.js inlines the value into the
# client bundle — the cinematic feed/series UIs fetch from this base.
NEXT_PUBLIC_API_URL=https://your-railway-api.up.railway.app
